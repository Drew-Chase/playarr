info:
  name: Get Sonarr quality profiles
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/sonarr/qualityprofile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
