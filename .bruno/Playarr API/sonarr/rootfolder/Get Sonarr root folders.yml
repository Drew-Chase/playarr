info:
  name: Get Sonarr root folders
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/sonarr/rootfolder"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
