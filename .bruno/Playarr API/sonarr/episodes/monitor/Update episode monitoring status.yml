info:
  name: Update episode monitoring status
  type: http
  seq: 1

http:
  method: PUT
  url: "{{baseUrl}}/sonarr/episode/monitor"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
