info:
  name: Add/request a TV show in Sonarr
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/sonarr/series"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Series added successfully
    request:
      url: "{{baseUrl}}/sonarr/series"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
  - name: 400 Response
    description: Sonarr rejected the request
    request:
      url: "{{baseUrl}}/sonarr/series"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      statusText: Bad Request
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
  - name: 500 Response
    description: Internal server error
    request:
      url: "{{baseUrl}}/sonarr/series"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      statusText: Internal Server Error
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "error": "",
            "status": 0
          }
