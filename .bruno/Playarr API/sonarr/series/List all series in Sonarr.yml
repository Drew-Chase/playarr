info:
  name: List all series in Sonarr
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/sonarr/series"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
