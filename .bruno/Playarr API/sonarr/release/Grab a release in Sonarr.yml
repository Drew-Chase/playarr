info:
  name: Grab a release in Sonarr
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/sonarr/release"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
