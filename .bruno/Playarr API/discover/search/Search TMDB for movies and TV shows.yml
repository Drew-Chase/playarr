info:
  name: Search TMDB for movies and TV shows
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/discover/search"
  params:
    - name: q
      value: ""
      enabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
