info:
  name: Get TMDB TV show details
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/discover/tv/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
