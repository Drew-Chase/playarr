info:
  name: Get TMDB season details
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/discover/tv/:id/season/:season_number"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
