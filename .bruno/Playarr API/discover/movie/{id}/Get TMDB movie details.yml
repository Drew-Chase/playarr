info:
  name: Get TMDB movie details
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/discover/movie/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
