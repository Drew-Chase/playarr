info:
  name: Stream a YouTube video
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/discover/youtube-stream/:video_id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
