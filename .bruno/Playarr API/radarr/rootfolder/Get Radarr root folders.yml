info:
  name: Get Radarr root folders
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/radarr/rootfolder"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
