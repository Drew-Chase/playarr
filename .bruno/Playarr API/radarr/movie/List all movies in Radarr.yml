info:
  name: List all movies in Radarr
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/radarr/movie"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
