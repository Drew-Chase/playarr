info:
  name: Search for movies in Radarr
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/radarr/lookup"
  params:
    - name: term
      value: ""
      type: query
      description: Search term (title or TMDB ID)
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Array of movie search results from Radarr
    request:
      url: "{{baseUrl}}/radarr/lookup"
      method: GET
      params:
        - name: term
          value: ""
          type: query
          description: Search term (title or TMDB ID)
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {}
          ]
  - name: 500 Response
    description: Internal server error
    request:
      url: "{{baseUrl}}/radarr/lookup"
      method: GET
      params:
        - name: term
          value: ""
          type: query
          description: Search term (title or TMDB ID)
    response:
      statusText: Internal Server Error
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "error": "",
            "status": 0
          }
