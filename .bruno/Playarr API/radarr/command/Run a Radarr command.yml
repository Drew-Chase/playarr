info:
  name: Run a Radarr command
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/radarr/command"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
