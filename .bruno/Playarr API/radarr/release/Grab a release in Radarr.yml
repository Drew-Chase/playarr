info:
  name: Grab a release in Radarr
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/radarr/release"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
