info:
  name: Get service URLs and Plex machine ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/service-urls"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
