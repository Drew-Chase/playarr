info:
  name: Check if guest access is available
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/auth/guest"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
