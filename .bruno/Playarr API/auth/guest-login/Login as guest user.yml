info:
  name: Login as guest user
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/auth/guest-login"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
