info:
  name: Upload subtitle to Plex
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/subtitles/upload-to-plex"
  body:
    type: json
    data: |-
      {
        "file_id": 0,
        "rating_key": "",
        "language_code": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
