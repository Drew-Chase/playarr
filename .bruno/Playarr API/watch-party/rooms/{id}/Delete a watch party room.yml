info:
  name: Delete a watch party room
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/watch-party/rooms/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
