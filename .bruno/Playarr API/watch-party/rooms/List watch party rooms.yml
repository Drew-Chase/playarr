info:
  name: List watch party rooms
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/watch-party/rooms"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
