info:
  name: Join room by invite code
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/watch-party/join/:code"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
