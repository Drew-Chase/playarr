info:
  name: List Plex users
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/plex/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
