info:
  name: Initial server setup
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/setup"
  body:
    type: json
    data: |-
      {
        "plex_url": "",
        "sonarr": {
          "url": "",
          "api_key": ""
        },
        "radarr": {
          "url": "",
          "api_key": ""
        },
        "download_clients": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
