info:
  name: Get server setup status
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
