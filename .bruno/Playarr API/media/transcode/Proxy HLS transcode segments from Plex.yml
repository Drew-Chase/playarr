info:
  name: Proxy HLS transcode segments from Plex
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/media/transcode/:path"
  params:
    - name: path
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
