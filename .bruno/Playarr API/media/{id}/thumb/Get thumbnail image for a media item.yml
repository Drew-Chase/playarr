info:
  name: Get thumbnail image for a media item
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/media/:id/thumb"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
