info:
  name: Get artwork image for a media item
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/media/:id/art"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
