info:
  name: Get on-deck episode for a show
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/media/:id/onDeck"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
