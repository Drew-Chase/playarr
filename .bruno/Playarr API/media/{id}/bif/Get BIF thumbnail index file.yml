info:
  name: Get BIF thumbnail index file
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/media/:id/bif"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
