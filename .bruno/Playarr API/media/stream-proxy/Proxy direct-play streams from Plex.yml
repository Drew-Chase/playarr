info:
  name: Proxy direct-play streams from Plex
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/media/stream-proxy/:path"
  params:
    - name: path
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
